name: field.blocks.image.name
icon: image
preview: image
fields:
    image:
        label: field.blocks.image.name
        type: files
        query: page('pictures').pictures.toFiles
        multiple: false
        image:
            back: black
        uploads: false
